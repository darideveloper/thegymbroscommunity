# Tasks: Update Branding and Content to Spanish (Gym Bros)

## 1. Global Branding
- [x] 1.1 Update `<title>` in `src/layouts/Layout.astro`.
- [x] 1.2 Update text in `src/components/atoms/TGCLogo.astro` (Ensure it remains "TGC").
- [x] 1.3 **Validation**: Verify logo and page title in browser.

## 2. Navigation & Header
- [x] 2.1 Update `menuItems` and `socialItems` in `src/components/organisms/Header.astro` to Spanish.
- [x] 2.2 Update `label` prop in `src/components/molecules/GlowDrawerToggle.tsx`.
- [x] 2.3 Update `title` and `aria-label` in `src/components/organisms/GlowDrawer.tsx`.
- [x] 2.4 Update `aria-label` in `src/components/molecules/GlowDrawerToggle.tsx`.
- [x] 2.5 **Validation**: Open menu and check all links and labels.

## 3. Reliability & Accessibility
- [x] 3.1 Update `aria-label` in `src/components/molecules/Countdown.jsx` to Spanish.
- [x] 3.2 Update error message in `src/components/atoms/ErrorBoundary.jsx` to Spanish.
- [x] 3.3 Update meta `<title>` in `src/layouts/Layout.astro`.
- [x] 3.4 **Validation**: Verify accessibility strings with dev tools and check error state by temporarily throwing an error in a component.

## 4. Hero & Products
- [x] 4.1 Update `TimeUnit` labels in `src/components/molecules/Countdown.jsx`.
- [x] 4.2 Update `TITLE_MAP` in `src/lib/api/products.ts` to Spanish.
- [x] 4.3 Update `texts` prop in `src/components/organisms/ProductShowcase.astro`.
- [x] 4.4 **Validation**: Check countdown and product titles in the showcase.

## 5. About Section
- [x] 5.1 Update `cardData` in `src/components/organisms/react-bits/MagicBento.jsx` to Spanish.
- [x] 5.2 **Validation**: Hover over Bento cards to verify new titles and descriptions.

## 6. Final Review
- [x] 6.1 Conduct a full site sweep to ensure no "placeholder" text remains.
- [x] 6.2 Verify "force and excitement" tone is consistent across all sections in Spanish.
