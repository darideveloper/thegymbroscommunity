# Tasks: Implement Custom Scrollbar

- [x] **Research & Prep**
  - [x] Confirm that `var(--primary)` and `var(--primary-foreground)` are the correct colors to use for the `halloween` theme.
  - [x] Check if `global.css` has any conflicting scrollbar styles.
- [x] **Implementation**
  - [x] Add the custom scrollbar CSS rules to `src/styles/global.css`.
  - [x] Apply styles to the `*` selector or specifically to `html` and `body`.
- [x] **Validation**
  - [x] Verify that the CSS is correctly applied and doesn't introduce any layout regressions.
  - [x] (Manual/Visual) Confirm that the scrollbar is thin and matches the primary color.
