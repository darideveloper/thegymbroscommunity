# Proposal: Add Logo to Top-Left Navigation

## Why
The project lacks consistent branding in the main navigation. Adding the logo to the top-left corner improves brand recognition and user navigation.

## What Changes
- Add the logo (`src/assets/logo.webp`) to the header area in `src/layouts/Layout.astro`.
- Style the logo for fixed positioning at the top-left corner.
