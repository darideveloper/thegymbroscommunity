## ADDED Requirements

### Requirement: Top-left logo placement
The logo from `src/assets/logo.webp` MUST be displayed in the top-left corner of the header area in the main layout.

#### Scenario: Logo presence
Given the user navigates to any page
When the page loads
Then the logo `src/assets/logo.webp` should be visible in the top-left corner.
