# Tasks: Add Meta Keywords and Author Tags

## Phase 1: Constants Enrichment
- [x] Add `SITE_KEYWORDS` and `BUSINESS_DATA.author` to `src/lib/constants.ts`.

## Phase 2: BaseSEO Update
- [x] Update `BaseSEO.astro` to include `keywords` and `author` meta tags with proper fallback logic.

## Phase 3: Validation
- [x] Build the site and verify `keywords` and `author` tags in `dist/index.html`.
